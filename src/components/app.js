import React from "react";
import styles from "../index.css";

const App = () => (
  <div>
    <h4>USAP Directory Home</h4>
    <button type="button" className={styles.button}>
      Login
    </button>
  </div>
);

export default App;
